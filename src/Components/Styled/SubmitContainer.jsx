import styled from "styled-components";

// Styled components like this allows for non repeating code and exports
export const SubmitContainer = styled.div`
  display: flex;
  gap: 30px;
  margin: 60px auto;
`;
